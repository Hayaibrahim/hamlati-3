<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.hamlati">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ratingPlace" />
        <activity android:name=".ratinghelth" />
        <activity android:name=".activity_login" />
        <activity android:name=".hamlah" />
        <activity android:name=".hamlat" />
        <activity android:name=".food" />
        <activity android:name=".Transportation" />
        <activity android:name=".administration" />
        <activity android:name=".forms"></activity>
    </application>

</manifest>